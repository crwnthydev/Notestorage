import { createStore } from 'vuex';
import { noteModule } from '@/store/noteModule';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    note: noteModule
  }
})
